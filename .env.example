# Groq API key — used by all 3 pipeline agents (diagnosis, channel, offer)
GROQ_API_KEY=your_groq_api_key_here

# Redis — used by worker_service for job queueing (docker-compose provides this)
REDIS_HOST=localhost
